from ....language import constant as lc





ATTRIBUTE_NAME_NAME          = "Name" if lc.ENG else "名前"
ATTRIBUTE_NAME_INFO          = "Info" if lc.ENG else "情報"

ATTRIBUTE_NAME_BODY_DAMAGE   = "Body Damage" if lc.ENG else "胴体ダメージ"
ATTRIBUTE_NAME_HEAD_DAMAGE   = "Head Damage" if lc.ENG else "ヘッドショットダメージ"
ATTRIBUTE_NAME_LEG_DAMAGE    = "Leg Damage"  if lc.ENG else "足ダメージ"
ATTRIBUTE_NAME_MAG_SIZE      = "Mag Size"    if lc.ENG else "装填数"
ATTRIBUTE_NAME_RPM           = "RPM"         if lc.ENG else "RPM"
ATTRIBUTE_NAME_BODY_DPS      = "Body DPS"    if lc.ENG else "胴体DPS"
ATTRIBUTE_NAME_UPS           = "UPS"         if lc.ENG else "UPS"

ATTRIBUTE_NAME_IGNITION_TIME = "Ignition Time" if lc.ENG else "着火時間"
